(()=>{
const BUILD='v0.8.0 · Build 20260813-18';
const cfg=window.PREHIP_SUPABASE_CONFIG;
const SESSION_KEY='prehip-supabase-session-v1';
let session=null,user=null,localSave=null,baseHome=null,syncTimer=null,syncing=false,cloudReady=false,lastError='';
const setVersion=()=>{const v=document.querySelector('.version');if(v)v.textContent=BUILD};
const authHeaders=(token=session?.access_token)=>({'apikey':cfg.publishableKey,'Authorization':`Bearer ${token||cfg.publishableKey}`,'Content-Type':'application/json'});
function storeSession(s){session=s||null;if(session)localStorage.setItem(SESSION_KEY,JSON.stringify(session));else localStorage.removeItem(SESSION_KEY)}
function loadSession(){try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch(e){return null}}
function parseAuthHash(){if(!location.hash||!location.hash.includes('access_token='))return null;const p=new URLSearchParams(location.hash.slice(1));const s={access_token:p.get('access_token'),refresh_token:p.get('refresh_token'),expires_in:Number(p.get('expires_in')||3600),token_type:p.get('token_type')||'bearer'};history.replaceState(null,'',location.pathname+location.search);return s.access_token&&s.refresh_token?s:null}
async function request(path,{method='GET',body,token=session?.access_token,headers={}}={}){const r=await fetch(cfg.url+path,{method,headers:{...authHeaders(token),...headers},body:body===undefined?undefined:JSON.stringify(body)});let data=null;try{data=await r.json()}catch(e){}if(r.status===401&&session?.refresh_token&&path!='/auth/v1/token?grant_type=refresh_token'){const ok=await refreshSession();if(ok)return request(path,{method,body,token:session.access_token,headers})}return {ok:r.ok,status:r.status,data}}
async function refreshSession(){if(!session?.refresh_token)return false;const r=await fetch(cfg.url+'/auth/v1/token?grant_type=refresh_token',{method:'POST',headers:authHeaders(cfg.publishableKey),body:JSON.stringify({refresh_token:session.refresh_token})});if(!r.ok){storeSession(null);user=null;cloudReady=false;return false}const d=await r.json();storeSession(d);session=d;user=d.user||user;return true}
async function hydrateUser(){if(!session?.access_token)return false;const r=await request('/auth/v1/user');if(!r.ok)return false;user=r.data;return true}
function normalize(raw){const d=raw&&typeof raw==='object'?raw:{};return {...state,...d,trainings:Array.isArray(d.trainings)?d.trainings:[],checks:d.checks&&typeof d.checks==='object'?d.checks:{},completed:Array.isArray(d.completed)?d.completed:[],weightHistory:Array.isArray(d.weightHistory)?d.weightHistory:[]}}
async function readCloud(){if(!user)return null;const r=await request(`/rest/v1/app_state?user_id=eq.${encodeURIComponent(user.id)}&select=state,updated_at`,{headers:{'Accept':'application/json'}});if(!r.ok)throw new Error(r.data?.message||`Cloud-Fehler ${r.status}`);return Array.isArray(r.data)&&r.data.length?r.data[0]:null}
async function writeCloud(force=false){if(!user||syncing||(!cloudReady&&!force))return;syncing=true;lastError='';decorate();try{const r=await request('/rest/v1/app_state?on_conflict=user_id',{method:'POST',body:{user_id:user.id,state,updated_at:new Date().toISOString()},headers:{'Prefer':'resolution=merge-duplicates,return=minimal'}});if(!r.ok)throw new Error(r.data?.message||`Cloud-Fehler ${r.status}`);cloudReady=true}catch(e){lastError=e.message||String(e)}finally{syncing=false;decorate()}}
function schedule(){if(!user||!cloudReady)return;clearTimeout(syncTimer);syncTimer=setTimeout(()=>writeCloud(),700)}
async function loadUserState(){if(!user)return;try{const remote=await readCloud();if(remote?.state){state=normalize(remote.state);localSave();cloudReady=true}else{cloudReady=true;await writeCloud(true)}lastError='';showPage(document.querySelector('.tabbar button.active')?.dataset.page||'home')}catch(e){lastError=e.message||String(e);cloudReady=false}decorate()}
function status(){if(user&&cloudReady)return{icon:'☁️',title:'Supabase Cloud aktiv',sub:user.email||'Angemeldet'};if(user&&lastError)return{icon:'⚠️',title:'Cloud-Sync Fehler',sub:lastError};if(user)return{icon:'☁️',title:'Cloud wird verbunden …',sub:user.email||''};return{icon:'🔐',title:'Cloud anmelden',sub:'Daten bleiben nach Cache-Löschen erhalten'}}
function card(){const s=status();return `<div class="section-title">Cloud</div><div class="card training-row" onclick="openPrehipCloud()"><div class="emoji">${s.icon}</div><div class="row-main"><strong>${s.title}</strong><small>${s.sub}</small></div><div class="chev">›</div></div>`}
function decorate(){const h=document.getElementById('prehip-cloud-card');if(h)h.innerHTML=card();setVersion()}
function msg(text,error=false){const e=document.getElementById('cloud-msg');if(e){e.textContent=text;e.style.color=error?'#b42318':'#667085'}}
window.openPrehipCloud=function(){if(user){modal(`<h2>preHIP Cloud</h2><div class="card" style="margin:0 0 12px"><strong>☁️ Synchronisiert</strong><br><small>${user.email||''}</small></div><p style="font-size:13px;color:#666;line-height:1.45">Supabase ist die dauerhafte Datenquelle. Auf dem iPhone bleibt zusätzlich ein Offline-Cache.</p><div id="cloud-msg" style="min-height:18px;font-size:12px;margin:8px 0"></div><button class="primary" onclick="prehipCloudSyncNow()">Jetzt synchronisieren</button><button style="width:100%;padding:13px;margin-top:8px;border-radius:12px;background:#f2f2ef;font-weight:700" onclick="prehipCloudReload()">Cloud-Daten neu laden</button><button style="width:100%;padding:13px;margin-top:8px;background:none;color:#b42318" onclick="prehipCloudSignOut()">Auf diesem Gerät abmelden</button>`)}else{modal(`<h2>preHIP Cloud</h2><p style="font-size:13px;color:#666;line-height:1.45">Einmal Konto anlegen oder anmelden. Nach einem Cache-Löschen genügt die erneute Anmeldung und deine Daten werden wiederhergestellt.</p><div class="field"><label>E-Mail</label><input id="cloud-email" type="email" autocomplete="email" inputmode="email"></div><div class="field"><label>Passwort</label><input id="cloud-password" type="password" autocomplete="current-password" minlength="8"></div><div id="cloud-msg" style="min-height:18px;font-size:12px;margin:8px 0"></div><button class="primary" onclick="prehipCloudLogin()">Anmelden</button><button style="width:100%;padding:13px;margin-top:8px;border-radius:12px;background:#f2f2ef;font-weight:700" onclick="prehipCloudSignup()">Konto anlegen</button>`)} }
function credentials(){return{email:document.getElementById('cloud-email')?.value.trim(),password:document.getElementById('cloud-password')?.value||''}}
window.prehipCloudLogin=async()=>{const c=credentials();if(!c.email||!c.password){msg('E-Mail und Passwort eingeben.',true);return}msg('Anmeldung …');const r=await request('/auth/v1/token?grant_type=password',{method:'POST',body:c,token:cfg.publishableKey});if(!r.ok){msg(r.data?.msg||r.data?.message||'Anmeldung fehlgeschlagen.',true);return}storeSession(r.data);session=r.data;user=r.data.user;await loadUserState();closeModal();showPage('home')};
window.prehipCloudSignup=async()=>{const c=credentials();if(!c.email||c.password.length<8){msg('E-Mail und mindestens 8 Zeichen Passwort eingeben.',true);return}msg('Konto wird angelegt …');const redirect=`${location.origin}${location.pathname}`;const r=await request(`/auth/v1/signup?redirect_to=${encodeURIComponent(redirect)}`,{method:'POST',body:c,token:cfg.publishableKey});if(!r.ok){msg(r.data?.msg||r.data?.message||'Registrierung fehlgeschlagen.',true);return}if(r.data?.access_token){storeSession(r.data);session=r.data;user=r.data.user;await loadUserState();closeModal();showPage('home')}else msg('Bestätigungs-E-Mail gesendet. Öffne den Link auf diesem iPhone.')};
window.prehipCloudSyncNow=async()=>{msg('Synchronisiere …');await writeCloud(true);msg(lastError||'Cloud ist aktuell.',!!lastError)};
window.prehipCloudReload=async()=>{msg('Lade Cloud-Daten …');try{const remote=await readCloud();if(remote?.state){state=normalize(remote.state);localSave();closeModal();showPage('home')}else msg('Noch keine Cloud-Daten vorhanden.',true)}catch(e){msg(e.message||String(e),true)}};
window.prehipCloudSignOut=async()=>{if(session?.access_token)await request('/auth/v1/logout',{method:'POST'});storeSession(null);session=null;user=null;cloudReady=false;closeModal();showPage('home')};
async function initAuth(){const fromHash=parseAuthHash();if(fromHash)storeSession(fromHash);session=fromHash||loadSession();if(session){await refreshSession();if(await hydrateUser())await loadUserState()}decorate()}
function init(){setVersion();if(!cfg||typeof state==='undefined'||typeof save!=='function'||typeof home!=='function'||typeof modal!=='function'){setTimeout(init,100);return}localSave=save;save=function(){localSave();schedule()};baseHome=home;home=function(c){baseHome(c);const page=c.querySelector('.page');if(page){const holder=document.createElement('div');holder.id='prehip-cloud-card';holder.innerHTML=card();const reset=page.querySelector('.dev-reset');if(reset)page.insertBefore(holder,reset);else page.appendChild(holder)}};window.addEventListener('online',()=>{if(user)writeCloud(true)});showPage('home');initAuth();setVersion()}
init();
})();